# 公开版本验证记录

验证日期：2026-09-24。

| 项目 | 结果 |
| --- | --- |
| 后端 `clean package` | 成功；36 项测试通过，无失败、错误或跳过 |
| 管理员初始化回归测试 | 指定初始密码、随机初始密码、已有管理员不被覆盖均通过 |
| 前端 `npm ci`、`npm run build` | 成功；类型检查和生产构建通过 |
| MySQL 全新数据库 | 23 个版本迁移全部成功；仅创建一个 `admin`，没有导入其他人员 |
| 登录与改密 | 管理员首次登录成功、要求改密、修改密码成功、旧密码失效 |
| 重启 | Flyway 校验通过；改变启动初始密码配置不会覆盖现有密码 |
| 源码与重建 JAR 检查 | 未发现已知内部名单的姓名、账号和密码哈希；V22/V23 无账号写入 |
| 文档 | 本地链接有效，中英文环境变量表一致 |

实际验证环境为 Windows、Java 21（按项目配置编译为 Java 17 字节码）、Maven 3.9.9，以及独立运行的 MySQL 5.7.26。没有访问内部业务数据库。本次没有单独在 Java 17 运行时或 MySQL 5.7.32 上重复验证；目标环境部署前应做环境验收。

前端构建存在单个产物超过 500 kB 的体积提示，不影响本次构建成功。未进行浏览器全流程、真实 AI 服务或 LibreOffice 转换测试，这些功能未在本次公开版本整理中修改。

敏感信息检查针对导出源码、已知人员标识、常见凭据格式与重建产物；不是对所有潜在敏感信息的绝对保证。内部仓库、旧远端历史及其历史制品不属于本公开仓库，也不随本次交付发布。
